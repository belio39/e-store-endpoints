"use strict";
// import Joi from "joi";
// export const OrderSchema = Joi.object({
//   user_id: Joi.string().required(),
//   user_id: Joi.string().required(),
//   total: Joi.number().min(0).max(10).required(),
// });
